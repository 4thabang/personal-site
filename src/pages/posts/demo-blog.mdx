---
slug: "demo-blog"
title: "Demo Blog"
date: 2021-07-15
author: Thabang
tags:
  - demo
  - blog
---

This is a demo blog post and is testing a new CMS in order to post new blog posts seamlessly.

### Here is some [Go](https://golang.org) code.
```golang
package main

import fmt

func main() {
    fmt.Println("Hello, world!")
}
```

### Here is some [TypeScript](https://typescript.com) code.
```TypeScript
const snippet = (name: string) => {
    console.log(name)
}

snippet("demo")
```

### Here is some [React + TS](https://reactjs.org) code.
```TypeScript
import * as React from 'react';

const { useState } = React;

const App = () => {
    const [count, setCount] = useState<number>(0)

    const increment = () => setCount(count + 1)
    const decrement = () => setCount(count - 1)

    return (
        <>
            <button onClick={increment}>+</button>
            <p>{count}</p>
            <button onClick={decrement}>-</button>
        </>
    )
}
```